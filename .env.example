# FastAPI Backend URL
# This should point to your running FastAPI backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# Docker Configuration
# Image name and tag for the Docker build
DOCKER_IMAGE_NAME=computor-web
DOCKER_IMAGE_TAG=latest
# Optional: Docker registry (leave empty for local builds)
DOCKER_REGISTRY=
